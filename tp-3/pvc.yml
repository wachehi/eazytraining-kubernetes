apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: pvc
   labels:
     type: local
spec:
  storageClassName: manuel
  accessModes:
    - ReadWriteOnce
  resources: 
     requests:
       storage: 100Mi
           
